version: '3.8'

services:

  ems-service:
    # restart: always
    build: .
    container_name: ems-service
    # volumes:
    #   - ./backend/Test:/app
    ports:
      - "3000:3000"
    # environment:
      # - POSTGRES_NAME=postgres
      # - POSTGRES_USER=postgres
      # - POSTGRES_PASSWORD=postgres
    networks:
      - service-network

networks:
  service-network:
    attachable: true
